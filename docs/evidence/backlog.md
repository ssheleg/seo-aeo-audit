# Board

The work-list between runs. Seeded 2026-08-10 during the fresh-eyes audit, which
found that every carry-over recorded on 2026-08-05 existed only in a session that
had ended — there was nowhere for a deferred item to survive.

Priority is computed, not asserted: **`(impact × confidence) / effort`**, the same
formula the skill uses on a client site. `impact` 1–5 on what it costs an audit to
be wrong here; `confidence` 1.0 confirmed defect · 0.7 measured but scoped · 0.4
single observation · 0.2 hypothesis; `effort` 1–5 in engineering days including the
release.

Status: `open` · `in progress` · `done` · `dropped (why)`.

`Kind` separates the two things this board holds, because they are not the same
backlog. A **defect** is something already shipped that is not true, or an instrument
that reports a state it cannot tell apart from another; it competes with the release. A
**feature** is an instrument the skill does not have yet — a real gap, ranked by the same
formula, and deliberately behind every defect. Fourteen rows are features and they stay
open on purpose: truth about what shipped comes before what has not.

| id | Priority | Item | Why it matters | Kind | Source | Status |
|---|---|---|---|---|---|---|
| B-1 | 3.5 | Re-tier the corpus against a split `FIELD` / `CASE` vocabulary | `DECISIONS.md` 2026-08-05 rejected the split because it meant re-tiering every claim in twenty-one files against a closed vocabulary. The 2026-08-10 audit found the cost of *not* splitting: `FIELD` now covers a single undated anecdote and a named multi-account observation, and the triage formula weights both 0.4 | feature | carry-over 2026-08-05; ledger D31 | open  |
| B-2 | 2.3 | Wire `graphify` into the harvest so the next run queries a graph rather than grepping | The pipeline's own doctrine names the code graph as the first thing a harvest queries; this repo has none, so every run re-reads the corpus by hand — this one read ~4,000 lines of references to find contradictions a graph would surface as edges | feature | carry-over 2026-08-05 | open  |
| B-3 | 1.8 | Auto-detect knowledge REQs from a diff instead of listing them | Standing instruction #8 exists because the source-admission gate attached to the requirement list and not to the diff. A check that reads the diff for new figures and dates would retire the instruction | feature | carry-over 2026-08-05; retro #8 | open  |
| B-4 | 4.0 | A guard that a claim's date is present, per numeric claim | The tier/date sweep in this run was manual across five references. `benchmarks.md` has a source column now; prose claims elsewhere do not, and 36 undated rows were found by a script that could be kept | defect | ledger D31, D32 | open  |
| B-5 | 2.5 | Decide whether `gsc_pull.py` should paginate | It caps at the API row limit and now says so. Pagination would remove the caveat but multiplies quota use on large properties, and the beyond-30 band is the one the cap truncates | feature | ledger D8 | open  |
| B-6 | 2.0 | A second-window mode for `gsc_pull.py`, for `gsc-historic-<window>` rows | The CSV contract defines the label and the script pulls one window; the workaround (two runs with different `--days`) is documented but manual | feature | ledger D41 | open  |
| B-7 | 1.5 | Fixture for a truncated **gzip** response | The truncation flag is proven end-to-end for an uncompressed body and unit-tested for the salvage path; the two are not covered together | defect | ledger D3 | open  |
| B-8 | 3.0 | Re-read every claim admitted under the old `FIELD` bar | `DECISIONS.md` 2026-08-05 marks this **review** — no check knows which claims were admitted under which definition of the tier that gates the most admissions | defect | DOCMAP propagation matrix | open  |
| B-9 | 1.5 | A shared `coverage` contract every collector emits — requested / returned / dropped | Six instruments were each taught honesty separately: `_flat` has four copies, the "did anything arrive" predicate two, the exit semantics four. Nothing states the contract, so a seventh script starts from zero and the copies are only held together by a count in `validate.py` | defect | v0.13.0 plan root cause 4; v0.14.0 M1 | open  |
| B-10 | 0.7 | Make the code graph's incremental mode usable in this repo | `detect_incremental` reports 72 of 72 files changed against either root, so `--update` cannot tell what moved; and re-extracting a file gives its entities new ids, which orphaned a hyperedge and pushed dangling endpoints from 6.3% to 10.3% between two builds one day apart. A graph that degrades each refresh is a false premise carrying a machine's authority | feature | 2026-08-10 stage 9 | open  |
| B-11 | 5.0 | `agent_traffic.py` — parse a server-log export, count AI/agent user agents, forward-confirm reverse DNS to drop spoofers | Track K tells the auditor to size agent demand before paying for agent surfaces, and hands them no instrument. Two live audits have now hit the same ceiling: every effect claim in K stays `HYPOTHESIS`, so the Experiments bucket cannot be ranked and the client is asked to buy files on faith. The highest-value missing tool in the skill | feature | 2026-08-14 privateclawd §5, §8; 2026-08-14 gap doc A1 | open  |
| B-12 | 2.0 | Extend the near-miss probe past root agent files | `agent-file-misnamed` catches `/llm.txt` standing in for `/llms.txt`. The same one-character class exists under `/.well-known/` and is unprobed, so a misnamed agent card still reports as plain absence — and absence sends the team to write a file they already have | defect | 2026-08-14 privateclawd §7 | open  |
| B-13 | 2.5 | `openapi_provenance` should ask whether the documented paths answer | It reads `servers[]` and template fingerprints, which catches the sample-spec case. A spec that genuinely describes this product's API but was never deployed passes every check, and the agent generating tools from it fails at call time rather than at read time | feature | 2026-08-14 privateclawd §7 | open  |
| B-14 | 3.0 | An instrument for a price restated in a flat agent file | `page_audit.py` has `jsonld-price-parity` for a page and now compares declared FAQ answers against the served body. A price restated in `llms.txt` has no instrument at all — the live case (a 30% annual discount the site's own tables put at 17%, in three places, on the surface built for machines) was found by reading, which does not scale | feature | 2026-08-14 privateclawd §4.4 | open  |
| B-15 | 2.0 | Detect a CDN-managed robots block that contradicts the origin for a **named** agent | `robots-contradictory` fires on duplicate `User-agent: *` records and disagreeing `Content-Signal` lines. The commoner shape — a managed block allowing what the origin disallows for one named crawler, or the reverse — is not detected, and it is the shape that silently reverses a decision somebody made on purpose | feature | 2026-08-14 privateclawd §4.2 | open  |
| B-16 | 3.0 | Probe `HEAD` as well as `GET` on every advertised path | `GET /.well-known/agent-skills/prowl/SKILL.md` returned 200 `text/markdown` and `HEAD` on the same URL returned **405**. The script reported the path as present, because every probe it makes is a `GET`. Crawlers and link-checkers `HEAD` first, so the one client the path exists for sees it broken while the audit calls it healthy | defect | 2026-08-14 prowl §3 S1 | open  |
| B-17 | 1.4 | Compare a number on a third-party manifest against the same number on the site | The MCP registry's current record for the audited product advertised **360+** tools where the product ships **448** and its own repo says so at the same version. A public number maintained by hand had drifted on a surface no presence probe looks at, and it was found by reading. Scope it narrowly — where a registry record and the site state the same countable noun, report both rather than adjudicating | feature | 2026-08-14 prowl §3 S2 | open  |
| B-18 | 2.0 | Registry lookups — npm / PyPI / Homebrew, checking `homepage` and `repository` resolve back to the domain | Two live line items could not be settled without it, and settling them by hand **reversed** one: the grader claimed the CLI was mentioned in `llms.txt`, which mentions no CLI, while the package is published with the domain as its homepage. Second live audit to need this; deferred on effort now, not on evidence | feature | gap-doc A4; 2026-08-14 prowl §3 S3 | open  |
| B-19 | 3.0 | Check whether the product mentions its own published package | Follows B-18 and is one grep once it exists. A shipped, versioned CLI that the agent-facing manual never names is a discoverability defect that no presence probe can see, because both halves are individually present. The current instruments can only report the package (absent) or the manual (fine) | feature | 2026-08-14 prowl §3 S4 | open  |
| B-20 | 2.0 | Probe the skills.sh listing instead of routing it to a decision bucket | `skills.sh/api/search?q=` answered in one request and settled the item — only an unrelated vendor's skills, nothing official. K9 bucket 3 currently hands the auditor a business decision with no measurement under it, and a decision you can measure should be measured first | feature | 2026-08-14 prowl §3 S5 | open  |
| B-21 | 3.0 | Validate `plugin.json` against the agent-plugins.org schema, not just its presence | The audited product's manifests exist and are non-conformant — no `$schema` key. Both verdicts the skill can currently produce, "absent" and "present", would have been wrong, and "absent" would have sent the team to write a file they already have. Same failure class as B-12 | defect | 2026-08-14 prowl §3 S6 | open  |
| B-22 | 1.0 | Report the redirect's own status code, and carry the landing URL on every probe row | **Re-filed 2026-08-20 — the reporting this row asked for exists.** Every probe records where it landed (`agent_surface.py:371,385,405`), `landed_where` reads it (`:765-781`), and the entry-point block reports a bounce as its own finding (`:940-949`). What is still missing is narrower and two things: the redirect's **status code** — 307 says *temporary*, 301 says *moved*, and they carry opposite instructions to the team — which `urlopen` swallows because it follows the hop, so it needs a no-follow probe; and `note()`'s check rows carry `url` and the final `status` but not `final_url` (`:814-822`, rendered at `:1243-1247`), so outside the entry-point block a probe that redirected still reads as the page that was asked for | defect | gap-doc A8; 2026-08-14 prowl §3 S7; re-measured 2026-08-20 | open  |
| B-23 | 2.0 | Make the multi-template markup sweep the default, not the caveat | The one-URL caveat is printed and the ergonomics still push toward the root. On this site the homepage carried **nine** schema types and the inner templates three, none descriptive — so a root-only run would have **flattered** the site. That is the inverse of the usual false finding and the same defect, which is the argument that a printed caveat is not a control. Second live audit to hit it | defect | gap-doc A7; 2026-08-14 prowl §3 S8 | open  |
| B-24 | 5.0 | **done** — the report's Track coverage table is a closed vocabulary, seeded from the instruments | The instruments could already tell a clean result from a check that never looked (`url_inspection.py:236-250`, `page_audit.py:94-110`, `gsc_pull.py:524-527`, `preflight.py:_unattempted_property`); the deliverable could not. `templates/audit-report.template.md` offered a `Status` column with no vocabulary and a free-text "Not checked" table, and nothing read either — so a track that silently returned nothing rendered identically to a clean one, in the document a client pays for. Track K had no row at all, three releases after `SKILL.md` declared it | defect | manifesto M-40 (`manifesto.md:46`); conformance row SE-01 | done  |
| B-25 | 3.0 | **done** — `SECURITY.md`'s measured I/O surface is counted, and the count is read out of the document and run | It said **six** scripts and that the I/O grep "prints **22** lines and that is all of it" against a measured seven and 26, for four releases, in the section a reader consults *because* they will not read the code — and `agent_surface.py` had no row in the per-script table at all. Fixed to seven / 33 (the grep now also matches `os.environ`, which the producer block reads and the published pattern would otherwise have missed, so the "whole surface" claim was true of a smaller pattern than it named). `validate.py` reads the regex **out of** `SECURITY.md` and runs it, so a narrowed pattern breaks the count it claims; the same guard reads `SKILL.md`'s script inventory, which named a `sitemap_pull.py` that has never existed | defect | measured during SE-01; closed by SE-02 | done  |
| B-26 | 2.0 | Discover has no row in the coverage table, and it is not a track | `SKILL.md` is explicit that Discover is *not* one of the tracks and needs its own eleventh pass. The coverage table's denominator is now the declared track list, so Discover is neither `observed` nor `unlooked` there — a reader cannot tell whether it was checked. Either it earns a row outside the track block or the "Not checked" table has to name it by default | defect | found while closing B-24 | open  |
| B-27 | 1.0 | `SKILL.md`'s body needs the split its own budget line asks for | 4994 tokens against a 5000 budget and a 4750 house limit — six tokens of headroom, recomputed by `python3 test/validate.py` on every run, which prints it. Measured 2026-08-19 with `make-skill`'s `audit_skill.py --house`, a command that does not ship in this tree; its `len(body) / 3.9` estimator was vendored into the gate on 2026-08-20 so the figure is reproducible from this repository. B-24 wanted four lines in Step 4 and measured 5107, so the pointer went to `deliverable-templates.md` and `preflight.md` instead. That is the right home for an invocation, but the choice was forced rather than made. v0.22.0's own notes say the next section needs a split, not a trim | defect | v0.22.0 CHANGELOG; measured during SE-01 | open  |
| B-28 | 1.8 | **done** — `retro.md`'s run stamps are derived from tags, and its prune arithmetic is counted | The file says the stamps are "what makes the cold-retirement trigger computable" and listed four, the newest `v0.14.1` — **eighteen** releases behind HEAD, not five, and this row restated the wrong figure too. The stamp series is now derived from `git for-each-ref … refs/tags` and names which stamps carry a prune entry, because a release is not a run that re-checked the instructions. The cap arithmetic was wrong in the other direction: the prune log said "all eleven checked" and "the list stands at **eleven** — one over the cap" while **ten** instructions are live (twelve numbered, #5 and #6 struck through), so a breached cap was recorded that never happened. `validate.py` counts live `## N.` headings, reads the last `the list stands at **N**` figure, and refuses a newest stamp older than `plugin.json` | defect | measured during SE-01; closed by SE-03 | done  |
| B-29 | 4.5 | **done** — every payload names the execution that produced it, and the report says what expires it | No script emitted a version, a timestamp or a run id: `grep -n "__version__\\|observed_at\\|timestamp" scripts/*.py` returned nothing, so an audit deliverable could not say when it was produced, by what version, or against what arguments. That is worst here of anywhere in the family — an SEO audit is its most perishable evidence, a crawl result expires the moment the site or the algorithm moves, and a three-month-old audit was indistinguishable from today's. All seven scripts now carry a byte-identical producer block (`skill` · `script` · `observed_at` · `runtime` · `args` · `scope` · `actor` · `model` · `trace`), in `--format json` under `producer` **and** in the default markdown or text output; both report skeletons carry a seeded `## Provenance` block with four named invalidators | defect | manifesto M-32 (`manifesto.md:210` — the citation read `:206-208`, re-resolved 2026-08-20) + M-08; conformance row SE-02 | done  |
| B-30 | 1.2 | The `v0.22.0` ledger section has no `Confirmed` answer | Its third column held the command that was run, not one of the four values the ledger defines at the top of the file — so six shipped requirements have evidence and no answer to "has anyone watched this work?", which is the column's whole purpose. The header was renamed to `Evidence` / `When` on 2026-08-20 to say what the cells are rather than claim a column that was never filled, and `validate.py`'s tally guard therefore does not require a count for it. Re-tiering the six rows needs the evidence each one rests on re-read, not a guess | defect | found while writing the tally guard, SE-03 | open  |
| B-31 | 0.8 | `v0.18.0` has a changelog section and was never released | `git tag -l 'v0.18*'` is empty and npm's version list jumps `0.17.1` → `0.19.0`, so a `## v0.18.0 — 2026-08-14` heading names a release that does not exist. It inflates every denominator computed from changelog headings — including the ledger-coverage guard's "twenty-one releases at or above v0.13.0" — and a reader looking for what shipped in 0.18.0 finds notes for a version nobody can install | defect | found while deriving the run stamps, SE-03 | open  |
| B-32 | 2.2 | Sixteen releases at or above the ledger floor have no verification section | The ledger's own closing policy is "releases from v0.13.0 forward get a row each" and sixteen of twenty-one do not have one. They are now **declared and counted** rather than silently absent, and the newest release is enforced — but the sixteen stay unwritten, because writing them now means writing them from the changelog, which is what that same paragraph refuses. Closing this means either re-deriving each from its commits and CI runs, or deciding in `DECISIONS.md` that the floor moves and saying so | defect | found while writing the ledger-coverage guard, SE-03 | open  |

| B-33 | 2.0 | `plant` cannot tell its own guard from the anchor-liveness guard | This workflow refuses a plant whose literal is no longer in the file it names — correct, and it means **every** literal-anchored document plant makes `validate.py` fail for a second reason whether the guard under test works or not. `plant` asks only "does the validator now fail", so it reported `ok` for a run-stamp plant that lowered one stamp of several while the guard, which takes the newest, correctly did not fire. `plantq` (needle required) covers the nineteen new document plants; the 41 older calls are unchanged, so any one of them could be passing for the wrong reason and nothing would say so. Closing this is converting them and reading each refusal once (measured 2026-08-20 while writing SE-03's plants) | defect | SE-03 | open |
| B-34 | 1.0 | 2560 unattributable temp trees under this machine's `$TMPDIR` | `find "$TMPDIR" -maxdepth 4 -type f -path '*/copy/sub/b.sh' \\| wc -l` → **2560**; `find "$TMPDIR" -maxdepth 2 -type d -name 'tmp*' \\| wc -l` → **28343**; `du -sh "$TMPDIR"` → **7.0G** (2026-08-20). The plant-guard fixture that made them is byte-identical in four repositories of this family (md5 `623a086d10a04940573c31cbebb93e31`), so no one of them can prove the trees are its own. This repository was the last of the four still leaking and no longer does; the pile itself is **left in place**, because sweeping state you cannot prove you own is what `manifesto.md:366` refuses. Reported rather than closed, and the new prefix makes everything after today attributable by name (measured 2026-08-20; referred by the umbrella) | defect | SE-03 | dropped (not ours to sweep — reported, and the leak that fed it is closed) |
## How this file is used

Stage 0 of any run reads it and quotes the open count in the brief. Stage 10 gives
every unresolved ledger row a board id here, and re-derives the priorities. A row
that leaves without either a `done` or a stated reason is the thing this file exists
to prevent.
